{
  "name": "sudokubox",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "version-patch": "nodemon index.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npx lint-staged",
      "pre-push": "npm run version-patch"
    }
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  }
}
